# Base requirements
pandas>=1.5.3
openpyxl>=3.0.10
xlsxwriter>=3.0.9
python-dotenv>=0.19.0

# GUI requirements (already included in standard Python)
# tkinter (no installation needed)

# Optional for legacy Excel support
xlrd>=2.0.1
